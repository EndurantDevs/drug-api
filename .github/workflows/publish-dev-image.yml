name: Publish dev image

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    name: Disabled; build on dev server
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dev images are built on the dev server via the private deploy repository."
